# DEPRECATED — use repo-root railway.web.toml instead.
#
# The stock_ai web service must deploy from the REPO ROOT (not backend/),
# with Config file = railway.web.toml in Railway service settings.
# See backend/README.md.

[build]
builder = "NIXPACKS"

[deploy]
startCommand = "uvicorn main:app --host 0.0.0.0 --port $PORT"
numReplicas = 1
