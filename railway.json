{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r data_layer/requirements.txt"
  },
  "deploy": {
    "startCommand": "uvicorn data_layer.serve:app --host 0.0.0.0 --port $PORT",
    "numReplicas": 1
  }
}
