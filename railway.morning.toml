# DEPRECATED — fund manager cron runs inside stock_ai via fund_scheduler.py.
# Do not create a separate Railway service for this. Use railway.web.toml on stock_ai.

[build]
builder = "NIXPACKS"
buildCommand = "pip install -r backend/requirements.txt"

[deploy]
startCommand = "cd backend && FUND_MANAGER_FILL_PRICE=intention python -m scripts.run_morning_all_wolves"
numReplicas = 1
